version: 2.1

workflows:
  build:
    jobs:
      - build:
          context:
            - fcichajna
jobs:
  build:
    docker: 
      - image: cimg/node:14.10.1 # the primary container, where your job's commands are run
    steps:
      - run: echo Hello CirlceCI! && npm install && gatsby build && node deploy.js